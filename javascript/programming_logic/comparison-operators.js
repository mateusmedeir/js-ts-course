/* 
Comparison Operators 

> bigger than
>= bigger than or equal to
< less than
<= less than or equal to
== equal (value)
=== equal strict (value and type)
!= different (value)
!== different strict (value and type)
*/

const num1 = 10; // number
const num2 = 10; // number
const comp = num1 !== num2;

console.log(comp);